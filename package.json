{
  "name": "@runpoint-partners/versioning",
  "version": "1.0.0",
  "private": true,
  "description": "Deterministic git-derived versioning + commit-SHA provenance + parameterized deploy toolkit (rpv CLI + reusable GitHub workflow). Dependency-free.",
  "type": "commonjs",
  "engines": { "node": ">=18" },
  "bin": { "rpv": "index.js" },
  "exports": {
    ".": "./index.js",
    "./config": "./lib/config.js",
    "./runtime": "./lib/runtime.js",
    "./package.json": "./package.json"
  },
  "files": ["index.js", "lib/"],
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dependencies": {}
}
